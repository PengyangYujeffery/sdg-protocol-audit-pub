# This repository's claim is byte-level reproducibility, so line endings are pinned rather than
# left to each machine's git default. Without this, a Windows clone rewrites every file to CRLF and
# the "git status should be clean after regenerating" check in the README stops meaning anything.
* text=auto eol=lf
*.pdf binary
*.png binary
